# U-01 Unity可靠控制技术验收记录

## 候选范围

U-01实现独立的Unity运行程序集，消费Python权威控制与v2.2遥测，维护只读会话镜像，并返回ACK与帧末渲染回执。旧v1.2入口未改写，任务映射表负责把本记录绑定到候选提交。

## 验收结果

| 要求 | 结果 | 证据 |
|---|---|---|
| AC1 重复、乱序和重连保持事件身份 | PASS | EditMode测试与`evidence/U-01/network-fault-log.json` |
| AC2 旧帧不覆盖，Unity本地时钟不推进模块 | PASS | EditMode、PlayMode与`evidence/U-01/state-mirror-trace.json` |
| AC3 错误握手、断连、非法回执失败关闭 | PASS | EditMode测试与网络故障矩阵 |
| TCP帧上限与失败连接隔离 | PASS | EditMode测试覆盖含LF的1 MiB边界及旧socket必然释放 |
| 回执实例隔离与重发稳定性 | PASS | EditMode测试覆盖同实例稳定、跨实例不同ID |
| v2.2步骤身份消费规则 | PASS | 直接消费F-05全部遥测正例与负例fixture |
| golden交付序列 | PASS | 19条控制、19条ACK、12条合同回执，最终镜像为`Completed` |

## 复现方式

```powershell
& 'D:\UnityEngine\6000.4.9f1\Editor\Unity.exe' -batchmode -nographics -projectPath 'D:\Agent\03-SRP\02-技术研发\04-Unity视觉\SRP-Weather-Visual' -runTests -testPlatform editmode -testFilter 'SRP.U01.Tests' -testResults 'D:\Agent\03-SRP\03-测试与实验\evidence\U-01\editmode-results.xml'
& 'D:\UnityEngine\6000.4.9f1\Editor\Unity.exe' -batchmode -nographics -projectPath 'D:\Agent\03-SRP\02-技术研发\04-Unity视觉\SRP-Weather-Visual' -runTests -testPlatform playmode -testFilter 'SRP.U01.Tests' -testResults 'D:\Agent\03-SRP\03-测试与实验\evidence\U-01\playmode-results.xml'
python 'D:\Agent\03-SRP\03-测试与实验\evidence\U-01\verify_u01.py'
```

## 边界

ACK与回执序列是明确标记的合同golden fixture；运行时只有U-02等渲染方显式确认某事件已呈现后才生成成功回执，断线时保留原始消息等待重连发送。

本记录证明Unity可靠控制技术探针及其自动化证据成立，不扩大为U-02场景适配、正式Unity构建、TouchDesigner操作台或真实设备链已经完成。候选进入`IN_REVIEW`后仍需独立复查和真实第二人签收。
